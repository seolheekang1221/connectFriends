import React from "react";
import LogoutIcon from '@mui/icons-material/Logout';

const Logout = () => {
    return (
        <div>
            <h1>
                Welcome <span>logout</span>
            </h1>
        </div>
    );
};

export default Logout;